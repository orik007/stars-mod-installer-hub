# Manual installation override

1. Otevři `overrides/catalog_installation.json`.
2. Nech v `mods` jen mody, které chceš přepsat.
3. Uprav jejich `installationProcess` včetně `decisionReason`.
4. Commitni změnu do `main`.
5. Spusť workflow `Manual installation override`.
